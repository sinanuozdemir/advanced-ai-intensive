# Week 3 — agent_api
#
# Pinned versions match the rest of the course's `requirements.txt`. We pull
# the LLM + tool stack from the root requirements too (langchain, langgraph,
# pydantic) but list them here so this app stays installable in isolation.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
prometheus-client>=0.20.0
python-json-logger>=2.0.7
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# Workflow / LLM
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.50

# MCP wrapper (the FastMCP server under mcp_server/)
mcp>=1.2.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.24.0
respx>=0.21.0
